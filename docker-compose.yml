version: '3.8'

services:
  api:
    build: .
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - GIN_MODE=release
    volumes:
      - ./.env:/app/.env:ro
      - ./resources:/app/resources
    restart: unless-stopped
